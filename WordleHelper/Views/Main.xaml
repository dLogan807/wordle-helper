<Window
  x:Class="WordleHelper.Main"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="clr-namespace:WordleHelper.Controls"
  xmlns:custom="clr-namespace:PlaceholderTextBox;assembly=PlaceholderTextBox"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:WordleHelper"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:viewmodels="clr-namespace:WordleHelper.ViewModels"
  x:Name="WordleHelper"
  Title="WordleHelper"
  Width="450"
  Height="550"
  MinWidth="450"
  MinHeight="550"
  d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
  WindowStartupLocation="CenterScreen"
  mc:Ignorable="d"
>
  <Window.Resources>
    <!--  Delete button definition (extending base)  -->
    <Style x:Key="DeleteButton" BasedOn="{StaticResource BaseButton}" TargetType="Button">
      <Setter Property="FontSize" Value="14" />
      <Style.Resources>
        <Style TargetType="Border">
          <Setter Property="CornerRadius" Value="4" />
        </Style>
      </Style.Resources>
    </Style>
  </Window.Resources>
  <Grid>
    <Border Grid.ColumnSpan="2" Background="#121213" BorderBrush="#363636" BorderThickness="5" />
    <Grid Grid.ColumnSpan="2" Margin="15">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="7*" />
        <ColumnDefinition Width="3*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="3*" />
        <RowDefinition Height="28*" />
      </Grid.RowDefinitions>
      <DockPanel Grid.Row="0" Grid.Column="0">
        <custom:PlaceholderTextBox
          Width="200"
          Margin="0,0,10,0"
          Padding="5,2"
          VerticalAlignment="Center"
          FontSize="16"
          Placeholder="Enter guess"
          Text="{Binding TypedGuess, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
        >
          <TextBox.InputBindings>
            <KeyBinding Key="Return" Command="{Binding AddGuessCommand}" />
          </TextBox.InputBindings>
        </custom:PlaceholderTextBox>
        <Button
          Width="40"
          Margin="0,9"
          HorizontalAlignment="Left"
          VerticalContentAlignment="Center"
          Command="{Binding AddGuessCommand}"
          Content="Add"
          FontWeight="Bold"
          Style="{StaticResource ExecuteButton}"
        />
      </DockPanel>
      <ItemsControl Grid.Row="1" Grid.Column="0" Padding="5" ItemsSource="{Binding Model.Guesses}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <DockPanel Margin="0,0,0,10">
              <controls:LetterButtonUserControl Margin="0,0,10,0" Letter="{Binding Letters[0]}" />
              <controls:LetterButtonUserControl Margin="0,0,10,0" Letter="{Binding Letters[1]}" />
              <controls:LetterButtonUserControl Margin="0,0,10,0" Letter="{Binding Letters[2]}" />
              <controls:LetterButtonUserControl Margin="0,0,10,0" Letter="{Binding Letters[3]}" />
              <controls:LetterButtonUserControl Margin="0,0,10,0" Letter="{Binding Letters[4]}" />
              <Button
                Width="50"
                Background="#e03131"
                Command="{Binding Path=DataContext.RemoveGuessCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                CommandParameter="{Binding}"
                Content=""
                FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                FontWeight="Bold"
                Foreground="White"
                Style="{StaticResource DeleteButton}"
              />
            </DockPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
      <Button
        Grid.Column="1"
        Margin="0,9"
        Command="{Binding GetWordsCommand}"
        Content="Get Words"
        FontWeight="Bold"
        Foreground="White"
        Style="{StaticResource ExecuteButton}"
      />
      <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">
        <ItemsControl
          Padding="5,2"
          Background="#232323"
          ItemsSource="{Binding Model.Results}"
          Visibility="Visible"
        >
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock FontSize="20" Foreground="White" Text="{Binding WordString}" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
    </Grid>
  </Grid>
</Window>
