<Window
  x:Class="WordleHelper.Main"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:WordleHelper"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:viewmodels="clr-namespace:WordleHelper.ViewModels"
  x:Name="WordleHelper"
  Title="WordleHelper"
  Width="400"
  Height="550"
  d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
  WindowStartupLocation="CenterScreen"
  mc:Ignorable="d"
>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="93*" />
      <ColumnDefinition Width="7*" />
    </Grid.ColumnDefinitions>
    <Border Grid.ColumnSpan="2" Background="#282D33" BorderBrush="#3A4149" BorderThickness="5" />
    <Grid Grid.ColumnSpan="2">
      <Grid.Resources>
        <DataTemplate x:Key="DeleteButtonTemplate">
          <Button
            Command="{Binding Path=DataContext.RemoveGuessCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
            CommandParameter="{Binding}"
            Content="Remove"
          />
        </DataTemplate>
      </Grid.Resources>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="67*" />
        <ColumnDefinition Width="13*" />
      </Grid.ColumnDefinitions>
      <DataGrid
        x:Name="GuessDataGrid"
        Margin="10,77,64,188"
        AutoGenerateColumns="False"
        GridLinesVisibility="None"
        HeadersVisibility="None"
        ItemsSource="{Binding Guesses}"
      >
        <DataGrid.Columns>
          <DataGridTextColumn Width="30" Binding="{Binding Letters[0].Value}" IsReadOnly="True" />
          <DataGridTextColumn Width="30" Binding="{Binding Letters[1].Value}" IsReadOnly="True" />
          <DataGridTextColumn Width="30" Binding="{Binding Letters[2].Value}" IsReadOnly="True" />
          <DataGridTextColumn Width="30" Binding="{Binding Letters[3].Value}" IsReadOnly="True" />
          <DataGridTextColumn Width="30" Binding="{Binding Letters[4].Value}" IsReadOnly="True" />
          <DataGridTemplateColumn CellTemplate="{StaticResource DeleteButtonTemplate}" />
        </DataGrid.Columns>
      </DataGrid>
      <Button
        MinWidth="66"
        MinHeight="34"
        Margin="161,38,0,0"
        Padding="5"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        VerticalContentAlignment="Center"
        Command="{Binding AddGuessCommand}"
        Content="Add"
        FontSize="12"
      />
      <TextBox
        Width="138"
        Height="34"
        Margin="10,38,0,0"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        FontSize="12"
        Text="{Binding TypedGuess, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
      >
        <TextBox.InputBindings>
          <KeyBinding Key="Return" Command="{Binding AddGuessCommand}" />
        </TextBox.InputBindings>
      </TextBox>
    </Grid>
    <Button
      MinWidth="105"
      MinHeight="34"
      Margin="18,477,0,0"
      Padding="5"
      HorizontalAlignment="Left"
      VerticalAlignment="Top"
      VerticalContentAlignment="Center"
      Command="{Binding ShowWindowCommand}"
      Content="Show Window"
      FontSize="12"
    />
  </Grid>
</Window>
